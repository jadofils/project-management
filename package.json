{
  "name": "project-manager",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm install @nestjs/cli && npm run build",
    "start": "cd backend && node dist/main",
    "start:prod": "cd backend && node dist/main",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "lint": "cd backend && npm run lint && cd ../frontend && npm run lint",
    "typecheck": "cd backend && npm run typecheck && cd ../frontend && npm run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
