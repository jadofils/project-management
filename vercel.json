{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf _dist && cp -r frontend/dist _dist",
  "outputDirectory": "_dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
